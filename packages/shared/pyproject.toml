[project]
name = "cutgen-shared"
version = "0.1.0"
description = "Contrato entre core e agent: metrics_schema.json, sem logica, so definicao."
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/cutgen_shared"]
